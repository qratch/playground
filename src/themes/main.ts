import { createMuiTheme } from '@material-ui/core'

export const mainTheme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#5f55f3',
    },
    secondary: {
      main: '#ffffff',
    },
  },
})
